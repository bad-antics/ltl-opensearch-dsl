// examples/demo.ltl — runnable demo for ltl_opensearch_dsl

import std::io
use ltl_opensearch_dsl as lib

fn main() {
    io::println("ltl_opensearch_dsl demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}